fastapi==0.135.3  # was pinned to 0.115.6, but the actual venv had already drifted to this; documenting reality rather than an untested pin
uvicorn[standard]==0.34.0
asyncpg==0.29.0
python-dotenv==1.0.1
pydantic==2.13.4  # google-genai pulls a newer pydantic-core; documenting reality rather than an untested pin
numpy==1.26.4  # used by engines/knowledge_consolidation.py, independent of embeddings
scikit-learn>=1.3.0
cryptography>=42.0.0  # encrypts per-user LLM provider API keys at rest
httpx==0.28.1  # runtime: calls the Hugging Face embeddings API; also used by tests

# LLM verbalization layer — at least one required; provider selected via LLM_PROVIDER
# (or overridden per-user via /settings/llm — see app/api/settings.py)
openai>=1.50.0
anthropic>=0.40.0
google-genai>=0.3.0

# Embeddings: sentence-transformers/all-MiniLM-L6-v2 via Hugging Face's hosted
# Inference API (app/services/embedding.py) — no local torch/model weights,
# fits Render's free tier. Needs HF_API_TOKEN. httpx below is the client.

# Content Extraction (optional — install if using ScrapeGraph provider)
# scrapegraphai>=2.0.0

# --- dev / test ---
pytest>=8.0.0
pytest-asyncio>=0.24.0
