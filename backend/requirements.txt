fastapi==0.115.6
uvicorn[standard]==0.34.0
asyncpg==0.29.0
sentence-transformers==5.3.0
python-dotenv==1.0.1
pydantic==2.10.6
numpy==1.26.4  # torch==2.3.0 (pulled in by sentence-transformers) is built against numpy 1.x's C ABI; numpy 2.x breaks tensor.numpy() with "Numpy is not available"
scikit-learn>=1.3.0

# LLM verbalization layer — at least one required; provider selected via LLM_PROVIDER
openai>=1.50.0
anthropic>=0.40.0

# Content Extraction (optional — install if using ScrapeGraph provider)
# scrapegraphai>=2.0.0

# --- dev / test ---
pytest>=8.0.0
pytest-asyncio>=0.24.0
